To enable the backend to authenticate and encrypt traffic certificates are needed. To install the certificates
just build the InstallCertificate project by right clicking it and selecting Build.

Note! This project is not part of the regular build, you must explicitly request a build.
